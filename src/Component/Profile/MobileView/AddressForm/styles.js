import styled from "styled-components";
import { Container } from "@material-ui/core";
import { base_spacing } from "../../../../Assets/style-var";

export const AddressFormWrapper = styled(Container)`
  margin-top: ${base_spacing}px;
`;
