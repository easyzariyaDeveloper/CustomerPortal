import React from "react";
import {GoogleLoginButton, GoogleIcon} from "../style";

export default function GoogleLogin(props){
    return <GoogleLoginButton>
        <GoogleIcon/>
        Continue With Google
    </GoogleLoginButton>
}